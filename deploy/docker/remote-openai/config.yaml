# Magec - OpenAI cloud configuration
#
# This file configures server infrastructure only.
# Agents, backends, MCPs, and memory are managed via the Admin UI at http://localhost:8081

server:
  host: 0.0.0.0
  port: 8080
  adminPort: 8081

log:
  level: info
  format: console
